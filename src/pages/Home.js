import { Box } from "@chakra-ui/react";
import ProductsContainer from "../components/ProductsContainer";

const Home = () => {
  return (
    <Box>
      <ProductsContainer />
    </Box>
  );
};

export default Home;
